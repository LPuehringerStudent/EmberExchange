import { ServiceBase } from '../../backend/services/service-base';

describe('ServiceBase', () => {
  it.todo('needs to be rewritten for PostgreSQL async API (executeStmt now returns Promise<[boolean, number]>)');
});
