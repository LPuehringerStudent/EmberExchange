import { StoveService } from '../../backend/services/stove-service';

describe('StoveService', () => {
  it.todo('needs to be rewritten for PostgreSQL async API (was mock-based with missing await on async methods)');
});
