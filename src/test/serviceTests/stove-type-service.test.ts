import { StoveTypeService } from '../../backend/services/stove-type-service';

describe('StoveTypeService', () => {
  it.todo('needs to be rewritten for PostgreSQL async API (was mock-based with outdated return types)');
});
