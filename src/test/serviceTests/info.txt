TODO: All service unit tests were placeholdered after migrating from SQLite to PostgreSQL.
The previous mock-based tests used synchronous APIs that no longer match the async Unit/Statement interface.

When rewriting:
  1. MockUnit.prepare should return MockStatements with async jest.fn().mockResolvedValue(...)
  2. All service method calls need await
  3. executeStmt returns Promise<[boolean, number]> not [boolean, number]
  4. Consider using integration tests against a real test DB instead of heavy mocking
